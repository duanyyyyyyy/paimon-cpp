f0:int,f1:string,f2:int,f3:double,f4:string
bucket count: 1
merge-engine: aggregation
fields.default-aggregate-function: min
deletion-vectors.enabled: true
lookup.remote-file.enabled: true
lookup.remote-file.level-threshold: 1

padding = 'X' * 2048

Msgs:
snapshot-1
Add(100, "Alice", 3, 1.5, padding);
Add(200, "Bob",   5, 2.5, padding);
Add(300, "Carol", 1, 3.5, padding);
Add(400, "Dave",  4, 4.5, padding);
Add(500, "Eve",   2, 5.5, padding);

snapshot-2
No more data

snapshot-3
compact with updrate file to level-5

snapshot-4
Add(50, "Alice", 3, 0.5, "a1");
Add(300, "Bob",   5, 3.5, "b1");

snapshot-5
Add(80, "Alice", 3, 1.0, "a2");
Add(150, "Carol", 1, 1.5, "c1");
